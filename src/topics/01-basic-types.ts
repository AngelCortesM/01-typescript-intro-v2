const name = "John Doe";
let hpPoints: number | 'FULL' = 95;
const isAlive: boolean = true;
const skills: string[] = ['JavaScript', 'TypeScript', 'React'];
hpPoints = 'FULL';

console.log({ name, hpPoints, isAlive, skills });
export{}